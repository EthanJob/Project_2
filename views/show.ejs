<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Meme</title>
  </head>
  <body>
    <header>
      <% include ./partials/header.ejs %>
    </header>

    <h3>Posted by <%= oneMeme.posted_by %></h3>
    <img src="<%= oneMeme.url %>" alt="meme image">

    <h2>Comments</h2>
    <hr>

    <% for (let comment of comments) { %>
      <h3><%= comment.author %></h3>
      <p><%= comment.content %></p>
      <hr>
    <% } %>

    <h3>Add a Comment</h3>

    <form action="/comments" method="post">
      <input type="text" name="author" placeholder="Author" value="Anonymous"/><br>
      <input type="text" name="content" placeholder="Comment"/><br>
      <input type="hidden" name="meme" value="<%= oneMeme._id %>"/>
      <button type="submit" name="button">Comment</button>
    </form>

    <hr>
    <a href="/home/meme/<%=oneMeme.id%>/edit">
      <button type="button">Edit Post</button>
    </a>

  </body>
</html>
